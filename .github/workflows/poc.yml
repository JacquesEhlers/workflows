name: Playwright POC

on:
  push:
  workflow_dispatch:

jobs:
  playwright-poc:
    runs-on: ubuntu-latest
    environment: test
    steps:
      # New step to print secrets
      - name: Print secrets
        run: |
          echo "TKORGANIZATION: ${{ secrets.TKORGANIZATION }}"
          echo "TKENVIRONMENT: ${{ secrets.TKENVIRONMENT }}"
          echo "TKTOKEN: ${{ secrets.TKTOKEN }}"
      - uses: kubeshop/setup-testkube@v1
        with:
          organization: NintexGlobal
          environment: "Test Control Cluster"
          token: tkcapi_3533e24da8697cda456e326c5eedde

      - run: |
          testkube run testworkflow playwright-poc -f
