name: Playwright POC

on:
  push:
  workflow_dispatch:

jobs:
  playwright-poc:
    runs-on: ubuntu-latest
    environment: test
    steps:
      - uses: kubeshop/setup-testkube@v1
        with:
          organization: nintexglobal
          environment: test-control-cluster
          token: tkcapi_647ea926fb80823b2924af4aa55781

      - run: |
          testkube run testworkflow playwright-poc -f
